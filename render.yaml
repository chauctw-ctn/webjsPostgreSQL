services:
  - type: web
    name: camau-water-monitoring
    runtime: node
    buildCommand: npm install && npm rebuild sqlite3
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
